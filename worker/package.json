{
  "name": "skydive-helper-burble-cron",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker cron that triggers the app's background Burble manifest sync",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
